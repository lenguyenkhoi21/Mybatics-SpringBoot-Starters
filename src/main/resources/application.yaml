spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mybatics

mybatis:
  mapper-locations: mybatis/mapper/**

logging:
  level:
    com:
      example:
        mybaticsspringbootstarters:
          mapper: trace
    org:
      springframework:
        jdbc:
          datasource:
            DataSourceTransactionManager: trace
        transaction: trace
      mybatis:
        spring:
          transaction: trace


